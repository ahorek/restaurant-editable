<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= t(:restaurant_name) %> - admin dashboard</title>
    <%= render 'layouts/header_scripts' %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= yield :head %>
  </head>

  <body>
    <%= render "layouts/admin/main_nav" %>
    <div class="container">
      <%= render "layouts/application/flash_messages" %>
      <%= yield %>
      <%= render 'layouts/bottom_buttons' %>
      <div id="ajax-modal" title=""><p></p></div>
    </div>
  </body>
</html>
